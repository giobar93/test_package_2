import React from "react";
type Props = {
    heigth?: number;
    width?: number;
    radius?: number;
    color?: string;
    visible?: boolean;
};
export default function Loader({ heigth, width, radius, color, visible }: Props): React.JSX.Element;
export {};
